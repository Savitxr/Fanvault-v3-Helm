apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "commerce-service.fullname" . }}-config
  labels:
    {{- include "commerce-service.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

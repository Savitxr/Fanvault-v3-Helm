apiVersion: v1
kind: Service
metadata:
  name: {{ include "commerce-service.fullname" . }}
  labels:
    {{- include "commerce-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commerce-service.selectorLabels" . | nindent 4 }}
